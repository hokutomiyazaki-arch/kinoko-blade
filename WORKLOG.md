# WORKLOG — kinoko-blade（菌血闘技 / KINOKO BLADE）

gitに残らない行為だけを書く（ファイルの変更は git log にある）。
書式: `- YYYY-MM-DD 何を / 相手・宛先 / 結果 → 次のアクション`

## 決めたこと

- **2026-08-15 public にする。** 理由は「人に見せるから」（CEO発言）。
  既定は private だが、依存ゼロの単体HTMLなので **GitHub Pages でURLを渡すだけで遊べる**のが
  見せる手段として一番速い。ZIPを送る・手元で開かせる、は却下（相手の手間が増える）。
- **2026-08-15 置き場所は `~/dev/kinoko-blade`。** 最初 `~/kinoko-blade`（ホーム直下）に作ったが、
  早見表に載る置き場は `~/dev/` `~/Desktop/` `~/FNT-Apps/` の3つと決めてあるので移動した。
  ホーム直下は表から漏れて次回迷う。
- **2026-08-15 `balance-sim.js` を `tools/` に移した。** README が `node tools/balance-sim.js` と
  書いているのにファイルはルートにあり、**READMEの通りに打つと動かない**状態だった。
  READMEを直すのではなくファイルを動かした（READMEの構成が意図した形なので）。
- **2026-08-15 バランス数値の二重管理は残したまま公開。** `index.html` の `SPECIES` と
  `tools/balance-sim.js` の配列が同じ値を手動同期している。片方だけ直すとシムが嘘をつく。
  `balance.json` への一元化は README の TODO にあるとおり次の一手（今回はやらない）。

- **2026-08-15 キャラクターの見た目を設定画に合わせて描き直した。** 渡された設定画5枚
  （水色・藤・若草・桃の剣士＋撃沈の姿勢）が新しい正。**画像ファイルは足していない。**
  設定画はチャット添付でリポジトリに実体がなく、また `index.html` 1枚で動く（＝ビルドも
  アセット読み込みもない）ことがこのゲームの配布手段そのものなので、canvas のベクタ描画で
  再現した。PNGを正にしたいなら `assets/` に置いてもらえば `drawImage` に切り替える。
- **2026-08-15 撃沈の姿勢に1.1秒の間を入れた。** 5枚目の設定画（×目）は勝敗が決した瞬間の絵
  なのに、従来は `finish()` が即座に結果画面へ切り替えていて**一度も表示されない**位置だった。
  時間切れ決着では見せる絵がないので0.26秒に短縮している。
- **2026-08-15 バランス数値には一切触れていない。** 見た目だけの変更なので
  `tools/balance-sim.js` との二重管理（下記）は今回も動かしていない。相性表は有効なまま。
  → この判断は同日の飛び道具追加で失効。数値は下記のとおり作り直した。

- **2026-08-15 飛び道具を全キャラに入れた。防御に意味を持たせるため。** 移動が自動で
  避けられない以上、弾に対する答えは防御しかない＝防御が「使うと得な行動」になる。
  弾はキャラごとに軌道が違う（直進 / 高速 / 蛇行 / 山なり / 低速で居座る）。
- **2026-08-15 弾をガードすると防御ゲージが戻る（通常45% / ジャスト75%）。** これがないと
  「速い弾を連射して相手の防御ゲージを枯らす」だけで勝ててしまう（シムでエノキが平均67%）。
- **2026-08-15 防御ゲージの上限を2.0秒にした。** ベニテング2.6s・マツタケ2.4sは
  弾を受ける手段がないのと同義で、勝率が20〜30%まで落ちた。遅さの表現は guardTime と
  ジャスト受付に寄せ、ゲージ自体は2.0sで頭打ちにした。
- **2026-08-15 闘技場を横に詰めた分、速度・間合・弾速も同率(0.855)で縮めた。** 距離だけ
  縮めると交錯間隔 `2d/(v1+v2)` が変わってテンポが崩れる（実測でエノキが70%まで跳ねた）。
  空間だけの相似縮小にすれば時間の定数を一切触らずに済む、と分かったのが今回の肝。
- **2026-08-15 シムの乱数に種を固定した。** 8/15のログに「シムは乱数なので試行ごとに振れる」と
  書いたが、READMEに載せる表が回すたび変わるのは検証として弱い。種固定で再現するようにした。
  併せて `require()` で読めるようにし、調整スクリプトから叩けるようにした。

- **2026-08-15 効果音とBGMを音声ファイルなしで実装した。** mp3を数本置くのが普通だが、
  それをやると「URLを渡すだけで遊べる単体HTML」ではなくなる。Web Audio で合成すれば
  ファイルは増えない。飛び道具は軌道ごとに鳴りを変えて、見ていなくても種類が分かるようにした。
- **2026-08-15 外部フォントの `<link>` がゲームの起動をブロックしていたのを直した。**
  音の検証中にヘッドレスで `readyState` が `loading` のまま進まないのを見つけた。
  `fonts.googleapis.com` が遅い・届かない環境では**スクリプトが実行されず画面が出ない**。
  `media="print" onload="this.media='all'"` にして非同期化した。回線の悪い所で人に見せる
  ゲームなので、これは音より重要な修正。

- **2026-08-15 敵にレベルを付けた（Lv.1開始、勝つと+1）。** 「今のCPUはまぁまぁ強い」ので
  従来値を **Lv.10** に置き、Lv.1 は反応0.60秒・ガード20%まで落とした。レベルで動かすのは
  CPUの中身だけで、キャラの性能表には触っていない（相性表を無効にしないため）。
- **2026-08-15 レベルの内挿を線形から0.5乗に変えた。** 線形だとシム上で Lv.1〜6 の勝率が
  1〜4%に潰れていて、**遊ぶ側から見て差が分からない**。0.5乗にして 0.5→3.3→10.5→25.2% と
  段階が付いた。`node tools/balance-sim.js levels` で測れるようにしてある。
- **2026-08-15 スペシャル武器はCPUに持たせない。** 勝利報酬なので非対称でよい、と判断した。
  対称性が要るのはキャラ同士の相性表だけで、そこには武器を含めない。
- **2026-08-15 一番レアな落雷菌を「距離無視だがガードで完全消滅」にした。** 強い技ほど
  読まれたときの損が大きい、という本編のルールを報酬側にも通すため。単に高火力にすると
  ガチャの当たり＝勝ち確定になって、防御を作った意味が消える。
- **2026-08-15 `showResult()` に二重実行の防止を入れた。** レベル加算とガチャ抽選が入って
  「1試合に1回だけ」でないと保存データが壊れる関数になったため。検証中に二重に呼んで気づいた。

## ログ

- 2026-08-15 Downloads の `files 2` から3ファイルを回収 / — / `~/dev/kinoko-blade` へ移動（コピーではなく移動。Downloads側は削除済み） → 完了
- 2026-08-15 秘密チェック（sk- / API_KEY / SECRET / TOKEN / password / メール / 電話番号）/ — / 検出ゼロ。public化して問題なし → 完了
- 2026-08-15 `node tools/balance-sim.js` を実走 / — / 動作OK。平均勝率 44.8%〜53.5%（README記載は 45.3%〜55.0%。シムは乱数なので試行ごとにこの程度は振れる。表は書いた時点の実測として残す） → 完了
- 2026-08-15 GitHub に public で作成 + Pages 公開 / — / 下記「公開URL」参照 → 人に見せる
- 2026-08-15 キャラ描き直しの確認 / — / headless Chromium で 5種 × 4姿勢を目視、
  25組の総当たりを各90秒ぶん描画して例外ゼロ、選択画面・戦闘画面・結果画面も実際に表示させて確認 → 完了
- 2026-08-15 飛び道具入りでバランス取り直し / — / 座標降下（scratchpadの使い捨てスクリプト）で
  飛ゲージ・威力を探索 → 平均勝率 46.0〜51.5%（旧 45.3〜55.0% より収束）。
  三すくみ エノキ→マツタケ→マイタケ→ベニテング→エノキ は維持 → 完了
- 2026-08-15 全画面レイアウトの確認 / — / 幅500〜1180で表示、3ボタンと闘技場が
  スクロールなしで収まること、選択画面は一覧だけスクロールし対戦開始ボタンが常に見えることを確認 → 完了
- 2026-08-15 音の実測 / — / CDP でヘッドレスChromeを動かし `OfflineAudioContext` に書き出して測定。
  16種すべて有音（peak 0.07〜0.30）、同時7音＋BGMの最悪ケースで 0.67（クリップなし）、
  ミュート時は完全に 0.000。25組の総当たりを音ありで回して例外ゼロ → 完了

- 2026-08-15 レベル・武器・ガチャの検証 / — / ヘッドレスで25項目を自動チェック（はずれ率
  29.9%/20000回、5種すべて出現、レア度順、各武器が命中し手持ちが減る、拘束1.6秒、
  上限5個、レベルと手持ちの保存、勝敗によるレベル増減、空手持ちで特ボタン無効）。
  さらに Lv.1/5/10/20 × 25組 = 100試合を武器を使いながら回して例外ゼロ → 完了
- 2026-08-15 検証用コピーから外部フォントの link を外した / — / 仮想時間がフォント取得待ちで
  止まり、テストが不安定だったため。製品側は非同期化済みなので影響なし → 完了

## 公開URL

- リポジトリ: https://github.com/hokutomiyazaki-arch/kinoko-blade
- 遊ぶ: https://hokutomiyazaki-arch.github.io/kinoko-blade/

## 待ち・未完了

- なし
